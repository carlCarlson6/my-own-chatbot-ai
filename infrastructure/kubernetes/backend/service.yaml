apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: chatbot-ai
  labels:
    app: backend
    project: my-own-chatbot-ai
spec:
  selector:
    app: backend
  ports:
    - name: http
      port: 5050
      targetPort: 5050
      protocol: TCP
  type: ClusterIP
